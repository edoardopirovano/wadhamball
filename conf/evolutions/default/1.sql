# --- !Ups

CREATE TABLE "EMAIL" ("EMAIL" VARCHAR PRIMARY KEY, "JOINDATE" timestamp NOT NULL);

CREATE TABLE "TICKET"("ID" serial NOT NULL PRIMARY KEY, "FIRSTNAME" VARCHAR NOT NULL, "LASTNAME" VARCHAR NOT NULL, "EMAIL" VARCHAR NOT NULL, "DEPOSITTRANSACTION" VARCHAR NOT NULL, "FINALTRANSACTION" VARCHAR, "ISDINING" BOOLEAN);

# --- !Downs

DROP TABLE "EMAIL";

DROP TABLE "TICKET";